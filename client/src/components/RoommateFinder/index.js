import React from 'react';
import Typography from '@mui/material/Typography';

const RoommateFinder = () => {
  return (
    <div>
      <Typography variant="h4">Roommate Finder</Typography>
      <Typography variant="body1">Find a roommate that fits your preferences.</Typography>
    </div>
  );
};

export default RoommateFinder;
